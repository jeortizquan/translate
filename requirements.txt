# Live Translate — Python dependencies
# pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
pydantic>=2.0.0
aiofiles>=23.0.0

# ── STT — whisper.cpp (binary, see setup.sh) ──────────────────────────────────
# (no pip package — whisper-stream is compiled from source)

# ── STT — faster-whisper 1.2.1 ────────────────────────────────────────────────
faster-whisper>=1.2.1
sounddevice>=0.4.6             # cross-platform microphone capture
numpy>=1.24.0                  # audio array processing

# Optional: Apple Silicon GPU acceleration via MLX
# pip install mlx-whisper       # uses Metal GPU on M-series chips

# ── TTS — Piper (Python library) ──────────────────────────────────────────────
piper-tts>=1.2.0

# ── Translation ────────────────────────────────────────────────────────────────
argostranslate>=1.9.6
httpx>=0.27.0                  # for Gemma/Ollama calls

# ── Audio device enumeration (fallback if sounddevice unavailable) ────────────
# macOS: brew install portaudio   before pip install pyaudio
pyaudio>=0.2.14

# ── Optional: Supertonic TTS ──────────────────────────────────────────────────
# pip install supertonic
